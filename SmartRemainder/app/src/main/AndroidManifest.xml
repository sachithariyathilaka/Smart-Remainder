<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sachith.smartremainder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartRemainder">
        <activity
            android:name=".activity.AddNewActivity"
            android:label="@string/title_activity_add_new"
            android:theme="@style/Theme.SmartRemainder.NoActionBar"></activity>
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.SmartRemainder.NoActionBar"></activity>
        <activity
            android:name=".activity.ConnectActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartRemainder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>